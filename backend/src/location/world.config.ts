// Overworld grid bounds — shared by Locations and Rifts, since both are
// occupants of the same map (see RiftWorldService.pickFreeCell).
export const WORLD_WIDTH = 7;
export const WORLD_HEIGHT = 5;
